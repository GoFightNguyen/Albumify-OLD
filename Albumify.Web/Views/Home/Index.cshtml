@model List<Albumify.Domain.Spotify.SpotifySearchAlbumResult>
@{
    ViewData["Title"] = "Home Page";
}

@*<div class="text-center">
    <h1 class="display-4">Spotify Authentication</h1>
    <p>
        <strong>Access Token:</strong> <code>@Model.AccessToken</code>
        <strong>Token Type:</strong> <code>@Model.TokenType</code>
        <strong>Expires In:</strong> <code>@Model.ExpiresIn</code>
    </p>
</div>*@

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model[0].Name)</th>
            <th>@Html.DisplayNameFor(model => model[0].NumberOfSongs)</th>
            <th>@Html.DisplayNameFor(model => model[0].ReleaseDate)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumberOfSongs)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReleaseDate)
                </td>
                @*<td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>*@
            </tr>
        }
    </tbody>
</table>
